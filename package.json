{
  "name": "repro",
  "version": "0.1.0",
  "description": "Autonomous Bug Reproduction CLI",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "tsc": "bunx tsc --noEmit",
    "tsc:watch": "bunx tsc --noEmit --watch"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12"
  }
}
